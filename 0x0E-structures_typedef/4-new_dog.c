#include <stdio.h>
#include "dog.h"

/**
 * new_dog - type dog
 * @name: name of the dog
 * @age: age of the dog
 * @owner: owner of the dog
 * Return: dog
 *
*/
dog_t *new_dog(char *name, float age, char *owner)
{
	dog_t *new_name;
	/**
	 * Still working on getting the correct code
	 */
	if (name == NULL || owner == NULL)
		return (NULL);
}
