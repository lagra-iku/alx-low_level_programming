#include "main.h"
/**
 * cap_string - capitalizes all words of a string.
 * @s: string to be manipulated
 * Return: 0
*/
char *cap_string(char *s)
{
	int i;
	char sep[] = " \t\n,;.!?\"(){}";

	for (i = 0; s[i] != '\0'; i++)
	{
		for (j = 0; sep[j] != '\0'; j++)
		{
		}
	}
	return (s);
}
